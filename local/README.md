# local/

This directory holds machine-specific notes and run-state that should never be committed.

Guidelines:
- Keep `local/` untracked and private.
- Do not store secrets here; use environment variables or a secret manager.
- Use placeholders for any operational examples (no real targets, no credentials).
